## 第22章：模型评估与优化 📈

### 22.1 模型评估的重要性

模型评估是机器学习工作流程中的关键步骤，它帮助我们：
- 了解模型的性能
- 比较不同模型
- 选择最佳模型
- 避免过拟合和欠拟合

### 22.2 评估指标

#### 22.2.1 分类评估指标

##### 22.2.1.1 准确率（Accuracy）

准确率是最常用的分类评估指标，它表示正确预测的样本数占总样本数的比例。

```python
from sklearn.metrics import accuracy_score

accuracy = accuracy_score(y_true, y_pred)
print(f"Accuracy: {accuracy}")
```

##### 22.2.1.2 精确率（Precision）和召回率（Recall）

- **精确率**：预测为正类的样本中实际为正类的比例
- **召回率**：实际为正类的样本中被正确预测的比例

```python
from sklearn.metrics import precision_score, recall_score

precision = precision_score(y_true, y_pred, average='weighted')
recall = recall_score(y_true, y_pred, average='weighted')
print(f"Precision: {precision}")
print(f"Recall: {recall}")
```

##### 22.2.1.3 F1 分数

F1 分数是精确率和召回率的调和平均，综合了两者的性能。

```python
from sklearn.metrics import f1_score

f1 = f1_score(y_true, y_pred, average='weighted')
print(f"F1 Score: {f1}")
```

##### 22.2.1.4 混淆矩阵

混淆矩阵展示了分类模型的预测结果，包括真正例（TP）、假正例（FP）、真负例（TN）和假负例（FN）。

```python
from sklearn.metrics import confusion_matrix
import seaborn as sns
import matplotlib.pyplot as plt

cm = confusion_matrix(y_true, y_pred)
plt.figure(figsize=(8, 6))
sns.heatmap(cm, annot=True, fmt='d', cmap='Blues')
plt.title('Confusion Matrix')
plt.xlabel('Predicted')
plt.ylabel('True')
plt.show()
```

##### 22.2.1.5 ROC 曲线和 AUC

ROC（Receiver Operating Characteristic）曲线是评估二分类模型性能的常用工具，AUC（Area Under Curve）是ROC曲线下的面积，值越大模型性能越好。

```python
from sklearn.metrics import roc_curve, roc_auc_score
import matplotlib.pyplot as plt

# 计算概率
y_pred_proba = model.predict_proba(X_test)[:, 1]

# 计算 ROC 曲线
fpr, tpr, thresholds = roc_curve(y_true, y_pred_proba)

# 计算 AUC
auc = roc_auc_score(y_true, y_pred_proba)

# 绘制 ROC 曲线
plt.plot(fpr, tpr, label=f'AUC = {auc:.2f}')
plt.plot([0, 1], [0, 1], 'k--')
plt.xlabel('False Positive Rate')
plt.ylabel('True Positive Rate')
plt.title('ROC Curve')
plt.legend()
plt.show()
```

##### 22.2.1.6 分类报告

分类报告提供了每个类别的精确率、召回率和F1分数。

```python
from sklearn.metrics import classification_report

report = classification_report(y_true, y_pred, target_names=class_names)
print(report)
```

#### 22.2.2 回归评估指标

##### 22.2.2.1 均方误差（MSE）

均方误差是预测值与真实值之差的平方的平均值。

```python
from sklearn.metrics import mean_squared_error

mse = mean_squared_error(y_true, y_pred)
print(f"MSE: {mse}")
```

##### 22.2.2.2 均方根误差（RMSE）

均方根误差是MSE的平方根，与目标变量的单位相同。

```python
import numpy as np
from sklearn.metrics import mean_squared_error

rmse = np.sqrt(mean_squared_error(y_true, y_pred))
print(f"RMSE: {rmse}")
```

##### 22.2.2.3 平均绝对误差（MAE）

平均绝对误差是预测值与真实值之差的绝对值的平均值。

```python
from sklearn.metrics import mean_absolute_error

mae = mean_absolute_error(y_true, y_pred)
print(f"MAE: {mae}")
```

##### 22.2.2.4 R² 分数

R² 分数表示模型解释的方差比例，越接近1越好。

```python
from sklearn.metrics import r2_score

r2 = r2_score(y_true, y_pred)
print(f"R² Score: {r2}")
```

### 22.3 交叉验证

交叉验证是一种评估模型性能的方法，通过将数据分为多个子集，轮流使用不同的子集作为验证集。

#### 22.3.1 K折交叉验证

```python
from sklearn.model_selection import cross_val_score

# K折交叉验证
scores = cross_val_score(model, X, y, cv=5, scoring='accuracy')  # 分类
# scores = cross_val_score(model, X, y, cv=5, scoring='r2')  # 回归

print(f"Cross-validation scores: {scores}")
print(f"Mean score: {scores.mean()}")
print(f"Standard deviation: {scores.std()}")
```

#### 22.3.2 留一交叉验证

留一交叉验证是K折交叉验证的特殊情况，其中K等于样本数。

```python
from sklearn.model_selection import LeaveOneOut

loo = LeaveOneOut()
scores = cross_val_score(model, X, y, cv=loo, scoring='accuracy')

print(f"Mean score: {scores.mean()}")
```

#### 22.3.3 留P交叉验证

留P交叉验证是指每次留下P个样本作为验证集。

```python
from sklearn.model_selection import LeavePOut

lpo = LeavePOut(p=2)
scores = cross_val_score(model, X, y, cv=lpo, scoring='accuracy')

print(f"Mean score: {scores.mean()}")
```

#### 22.3.4 分层K折交叉验证

分层K折交叉验证确保每个折中的类别分布与原始数据相同。

```python
from sklearn.model_selection import StratifiedKFold

skf = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
scores = cross_val_score(model, X, y, cv=skf, scoring='accuracy')

print(f"Cross-validation scores: {scores}")
print(f"Mean score: {scores.mean()}")
```

### 22.4 超参数调优

超参数是在模型训练前设置的参数，需要通过调优来找到最佳值。

#### 22.4.1 网格搜索

网格搜索是一种暴力搜索方法，它通过遍历所有可能的参数组合来找到最佳参数。

```python
from sklearn.model_selection import GridSearchCV

# 定义参数网格
param_grid = {
    'n_estimators': [100, 200, 300],
    'max_depth': [3, 5, 7, None],
    'min_samples_split': [2, 5, 10]
}

# 网格搜索
grid_search = GridSearchCV(
    estimator=RandomForestClassifier(random_state=42),
    param_grid=param_grid,
    cv=5,
    scoring='accuracy',
    n_jobs=-1  # 使用所有CPU核心
)

grid_search.fit(X_train, y_train)

# 最佳参数和分数
print(f"Best parameters: {grid_search.best_params_}")
print(f"Best score: {grid_search.best_score_}")

# 使用最佳模型
best_model = grid_search.best_estimator_
y_pred = best_model.predict(X_test)
```

#### 22.4.2 随机搜索

随机搜索是指在参数空间中随机采样进行搜索，适用于参数较多的情况。

```python
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import randint, uniform

# 定义参数分布
param_dist = {
    'n_estimators': randint(100, 500),
    'max_depth': [3, 5, 7, None],
    'min_samples_split': randint(2, 20),
    'min_samples_leaf': randint(1, 10),
    'max_features': uniform(0.1, 0.9)
}

# 随机搜索
random_search = RandomizedSearchCV(
    estimator=RandomForestClassifier(random_state=42),
    param_distributions=param_dist,
    n_iter=100,
    cv=5,
    scoring='accuracy',
    n_jobs=-1,
    random_state=42
)

random_search.fit(X_train, y_train)

# 最佳参数和分数
print(f"Best parameters: {random_search.best_params_}")
print(f"Best score: {random_search.best_score_}")
```

#### 22.4.3 贝叶斯优化

贝叶斯优化是一种更高效的超参数调优方法，它通过概率模型来指导搜索。

```python
from skopt import BayesSearchCV
from skopt.space import Real, Integer, Categorical

# 定义参数空间
param_space = {
    'n_estimators': Integer(100, 500),
    'max_depth': Integer(3, 10),
    'min_samples_split': Integer(2, 20),
    'min_samples_leaf': Integer(1, 10),
    'max_features': Real(0.1, 1.0)
}

# 贝叶斯优化
bayes_search = BayesSearchCV(
    estimator=RandomForestClassifier(random_state=42),
    search_spaces=param_space,
    n_iter=50,
    cv=5,
    scoring='accuracy',
    n_jobs=-1,
    random_state=42
)

bayes_search.fit(X_train, y_train)

# 最佳参数和分数
print(f"Best parameters: {bayes_search.best_params_}")
print(f"Best score: {bayes_search.best_score_}")
```

### 22.5 特征工程

特征工程是提高模型性能的重要步骤，包括特征选择、特征提取和特征转换。

#### 22.5.1 特征选择

特征选择是选择对预测有帮助的特征，减少维度，提高模型性能。

##### 22.5.1.1 过滤法

```python
from sklearn.feature_selection import SelectKBest, chi2, f_regression

# 分类问题
selector = SelectKBest(chi2, k=10)
X_new = selector.fit_transform(X, y)

# 回归问题
selector = SelectKBest(f_regression, k=10)
X_new = selector.fit_transform(X, y)
```

##### 22.5.1.2 包装法

```python
from sklearn.feature_selection import RFE
from sklearn.linear_model import LogisticRegression

# 递归特征消除
estimator = LogisticRegression()
selector = RFE(estimator, n_features_to_select=10, step=1)
X_new = selector.fit_transform(X, y)

# 查看选择的特征
selected_features = X.columns[selector.support_]
print(f"Selected features: {selected_features}")
```

##### 22.5.1.3 嵌入法

```python
from sklearn.linear_model import Lasso

# L1正则化自动选择特征
model = Lasso(alpha=0.1)
model.fit(X, y)

# 查看非零系数的特征
selected_features = X.columns[model.coef_ != 0]
print(f"Selected features: {selected_features}")
```

#### 22.5.2 特征提取

特征提取是从原始数据中提取新特征的过程。

##### 22.5.2.1 PCA

```python
from sklearn.decomposition import PCA

pca = PCA(n_components=10)
X_pca = pca.fit_transform(X)
```

##### 22.5.2.2 文本特征提取

```python
from sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer

# 词袋模型
vectorizer = CountVectorizer()
X_text = vectorizer.fit_transform(texts)

# TF-IDF
vectorizer = TfidfVectorizer()
X_text = vectorizer.fit_transform(texts)
```

#### 22.5.3 特征转换

特征转换是对特征进行预处理的过程，包括标准化、归一化、编码等。

```python
from sklearn.preprocessing import StandardScaler, MinMaxScaler, OneHotEncoder
from sklearn.compose import ColumnTransformer

# 标准化
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

# 归一化
scaler = MinMaxScaler()
X_scaled = scaler.fit_transform(X)

# 独热编码
ct = ColumnTransformer([
    ('onehot', OneHotEncoder(), [0])
], remainder='passthrough')
X_encoded = ct.fit_transform(X)
```

### 22.6 模型集成

模型集成是将多个模型的预测结果组合起来，以提高预测性能。

#### 22.6.1 投票法

```python
from sklearn.ensemble import VotingClassifier
from sklearn.linear_model import LogisticRegression
from sklearn.tree import DecisionTreeClassifier
from sklearn.svm import SVC

# 投票分类器
voting_clf = VotingClassifier(
    estimators=[
        ('lr', LogisticRegression()),
        ('dt', DecisionTreeClassifier()),
        ('svc', SVC(probability=True))
    ],
    voting='soft'  # 'hard' 或 'soft'
)

voting_clf.fit(X_train, y_train)
y_pred = voting_clf.predict(X_test)
```

#### 22.6.2 Bagging

```python
from sklearn.ensemble import BaggingClassifier
from sklearn.tree import DecisionTreeClassifier

# Bagging分类器
bagging_clf = BaggingClassifier(
    base_estimator=DecisionTreeClassifier(),
    n_estimators=100,
    random_state=42
)

bagging_clf.fit(X_train, y_train)
y_pred = bagging_clf.predict(X_test)
```

#### 22.6.3 Boosting

```python
from sklearn.ensemble import AdaBoostClassifier, GradientBoostingClassifier

# AdaBoost
ada_clf = AdaBoostClassifier(
    base_estimator=DecisionTreeClassifier(max_depth=1),
    n_estimators=100,
    learning_rate=0.1,
    random_state=42
)

ada_clf.fit(X_train, y_train)
y_pred = ada_clf.predict(X_test)

# Gradient Boosting
gb_clf = GradientBoostingClassifier(
    n_estimators=100,
    learning_rate=0.1,
    max_depth=3,
    random_state=42
)

gb_clf.fit(X_train, y_train)
y_pred = gb_clf.predict(X_test)
```

#### 22.6.4 Stacking

Stacking是一种更复杂的集成方法，它使用元模型来组合基础模型的预测。

```python
from sklearn.ensemble import StackingClassifier
from sklearn.linear_model import LogisticRegression, RidgeClassifier
from sklearn.tree import DecisionTreeClassifier
from sklearn.svm import SVC

# 基础模型
base_estimators = [
    ('lr', LogisticRegression()),
    ('dt', DecisionTreeClassifier()),
    ('svc', SVC(probability=True))
]

# 元模型
meta_estimator = RidgeClassifier()

# Stacking分类器
stacking_clf = StackingClassifier(
    estimators=base_estimators,
    final_estimator=meta_estimator,
    cv=5
)

stacking_clf.fit(X_train, y_train)
y_pred = stacking_clf.predict(X_test)
```

### 22.7 过拟合与欠拟合

#### 22.7.1 识别过拟合与欠拟合

- **过拟合**：模型在训练数据上表现很好，但在新数据上表现很差
- **欠拟合**：模型在训练数据和新数据上表现都很差

```python
import matplotlib.pyplot as plt

# 绘制学习曲线
def plot_learning_curve(estimator, X, y, cv=5):
    from sklearn.model_selection import learning_curve
    import numpy as np
    
    train_sizes, train_scores, test_scores = learning_curve(
        estimator, X, y, cv=cv, n_jobs=-1,
        train_sizes=np.linspace(0.1, 1.0, 10)
    )
    
    train_mean = np.mean(train_scores, axis=1)
    train_std = np.std(train_scores, axis=1)
    test_mean = np.mean(test_scores, axis=1)
    test_std = np.std(test_scores, axis=1)
    
    plt.figure(figsize=(10, 6))
    plt.fill_between(train_sizes, train_mean - train_std, train_mean + train_std, alpha=0.1, color='blue')
    plt.fill_between(train_sizes, test_mean - test_std, test_mean + test_std, alpha=0.1, color='green')
    plt.plot(train_sizes, train_mean, 'o-', color='blue', label='Training score')
    plt.plot(train_sizes, test_mean, 'o-', color='green', label='Cross-validation score')
    plt.title('Learning Curve')
    plt.xlabel('Training examples')
    plt.ylabel('Score')
    plt.legend(loc='best')
    plt.grid(True)
    plt.show()

# 绘制学习曲线
plot_learning_curve(model, X, y, cv=5)
```

#### 22.7.2 解决过拟合

- **增加数据**：获取更多的训练数据
- **减少特征**：删除不重要的特征
- **正则化**：使用 L1 或 L2 正则化
- **交叉验证**：使用交叉验证评估模型
- **集成学习**：使用 Bagging 或 Boosting 等集成方法
- **早停**：在训练过程中当验证集性能不再提升时停止训练

#### 22.7.3 解决欠拟合

- **增加模型复杂度**：使用更复杂的模型
- **增加特征**：添加更多的特征
- **减少正则化**：降低正则化强度
- **使用更高级的算法**：使用更强大的算法

### 22.8 模型选择

#### 22.8.1 模型比较

```python
from sklearn.model_selection import cross_val_score
from sklearn.linear_model import LogisticRegression
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import RandomForestClassifier
from sklearn.svm import SVC

# 定义模型
models = {
    'Logistic Regression': LogisticRegression(),
    'Decision Tree': DecisionTreeClassifier(),
    'Random Forest': RandomForestClassifier(),
    'SVM': SVC()
}

# 比较模型
results = {}
for name, model in models.items():
    scores = cross_val_score(model, X, y, cv=5, scoring='accuracy')
    results[name] = scores
    print(f"{name}: {scores.mean():.4f} ± {scores.std():.4f}")

# 可视化比较
import matplotlib.pyplot as plt
import numpy as np

plt.figure(figsize=(10, 6))
plt.boxplot(results.values(), labels=results.keys())
plt.title('Model Comparison')
plt.ylabel('Accuracy')
plt.ylim(0.7, 1.0)
plt.grid(True)
plt.show()
```

#### 22.8.2 模型保存与加载

```python
import joblib

# 保存模型
joblib.dump(model, 'model.pkl')

# 加载模型
loaded_model = joblib.load('model.pkl')

# 使用加载的模型
y_pred = loaded_model.predict(X_test)
```

### 22.9 模型部署

#### 22.9.1 模型部署的步骤

1. **模型训练**：在训练数据上训练模型
2. **模型评估**：在测试数据上评估模型性能
3. **模型保存**：将训练好的模型保存为文件
4. **模型加载**：在部署环境中加载模型
5. **模型服务**：将模型封装为API或服务
6. **监控与维护**：监控模型性能，定期更新模型

#### 22.9.2 模型部署的方法

- **本地部署**：在本地服务器上部署模型
- **云部署**：在云平台上部署模型（如AWS SageMaker、Azure ML、Google Cloud AI）
- **容器部署**：使用Docker容器部署模型
- **边缘部署**：在边缘设备上部署模型

#### 22.9.3 使用 Flask 部署模型

```python
from flask import Flask, request, jsonify
import joblib
import numpy as np

# 加载模型
model = joblib.load('model.pkl')

# 创建Flask应用
app = Flask(__name__)

# 定义API端点
@app.route('/predict', methods=['POST'])
def predict():
    # 获取请求数据
    data = request.get_json()
    
    # 转换数据
    features = np.array(data['features']).reshape(1, -1)
    
    # 预测
    prediction = model.predict(features)[0]
    
    # 返回结果
    return jsonify({'prediction': int(prediction)})

if __name__ == '__main__':
    app.run(debug=True, host='0.0.0.0', port=5000)
```

### 22.10 最佳实践

1. **数据质量**：确保数据的质量，处理缺失值和异常值
2. **特征工程**：重视特征工程，选择和创建有意义的特征
3. **模型选择**：根据问题类型选择合适的模型
4. **交叉验证**：使用交叉验证评估模型性能
5. **超参数调优**：使用网格搜索、随机搜索或贝叶斯优化调优超参数
6. **模型集成**：考虑使用模型集成提高性能
7. **过拟合防范**：采取措施防止过拟合
8. **模型部署**：选择合适的部署方式，确保模型能够在生产环境中正常运行
9. **监控维护**：定期监控模型性能，及时更新模型
10. **文档记录**：记录模型的训练过程、参数设置和评估结果