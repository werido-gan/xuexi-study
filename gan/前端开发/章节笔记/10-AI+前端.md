---
title: AI+前端
tags: [前端, AI, OpenAI, Copilot]
category: 前端开发
---

## 10. AI + 前端（加分项）

### 10.1 AI 在前端开发中的应用

```
[AI 辅助前端开发全景]

开发效率
├── GitHub Copilot / 通义灵码：代码自动补全
├── Cursor：AI原生IDE，可对话修改代码
└── Claude / ChatGPT：代码审查、解决Bug

产品功能
├── AI 对话界面（Chatbot）
├── 智能搜索（语义搜索）
├── AI图像生成（DALL-E / Stable Diffusion）
├── 语音识别（Web Speech API + Whisper）
└── 实时翻译

数据分析
├── 自然语言查询数据
└── 智能数据可视化
```

### 10.2 接入 AI API 实战

```javascript
// 接入 OpenAI API（流式响应，更好的用户体验）
async function* streamChat(messages) {
  const response = await fetch('https://api.openai.com/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${API_KEY}`
    },
    body: JSON.stringify({
      model: 'gpt-4',
      messages,
      stream: true
    })
  });
  
  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    
    const chunk = decoder.decode(value);
    const lines = chunk.split('\n').filter(line => line.startsWith('data: '));
    
    for (const line of lines) {
      const json = line.slice(6);
      if (json === '[DONE]') return;
      const data = JSON.parse(json);
      yield data.choices[0]?.delta?.content || '';
    }
  }
}

// React 组件中使用
function ChatBox() {
  const [messages, setMessages] = useState([]);
  const [input, setInput] = useState('');
  const [streaming, setStreaming] = useState('');
  
  const handleSend = async () => {
    const newMessages = [...messages, { role: 'user', content: input }];
    setMessages(newMessages);
    setInput('');
    
    let fullResponse = '';
    for await (const chunk of streamChat(newMessages)) {
      fullResponse += chunk;
      setStreaming(fullResponse);  // 实时更新显示
    }
    
    setMessages([...newMessages, { role: 'assistant', content: fullResponse }]);
    setStreaming('');
  };
  
  return (
    <div className="chat-box">
      {messages.map((msg, i) => (
        <div key={i} className={`message ${msg.role}`}>
          {msg.content}
        </div>
      ))}
      {streaming && <div className="message assistant">{streaming}▊</div>}
      <input value={input} onChange={e => setInput(e.target.value)} />
      <button onClick={handleSend}>发送</button>
    </div>
  );
}
```

---
